/**
 *
 * Intentionally left blank
 *
 * Until now, we do not know how to test a rule that uses templates.
 * We need to fake url() & apply() ["@angular-devkit/schematics" ]
 * to return a pre configured Source.
 *
 */

describe.skip('configure-prettier', () => {
  it('should be tested as unit', () => {});
});
